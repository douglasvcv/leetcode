var passThePillow = function(n, time) {
    let i = 1
    let second = 0
    let timeSecond = n.length
    while (timeSecond > 0) {
        timeSecond--
        second++
        i++
        if (n.length == second ) {
            second == 1
            i==1
        }
    }
    return i
};